{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Musician edit</h1>

    {{ form_start(edit_form, {'id': 'ze_babundle_musician_edit'}) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('musician') }}">
            Back to the list
        </a>
    </li>
    {{ form_end(edit_form) }}
    <li>{{ form(delete_form) }}</li>
</ul>
    <div id="builder"></div>
    <script>

        window.entity_id = {{ edit_form.vars.value.id }};
    </script>
    {% javascripts '@builder' %}
    <script type="text/javascript" src="{{ asset_url }}"></script>

    {% endjavascripts %}
{% endblock %}
