{% extends '::base.html.twig' %}
{% block container %}
    <div class="panel">
        {% if bands is defined%}
        <h4>Bands Searching For Musicians:</h4>
        {% embed'ZEBABundle::item_container.html.twig' with { 'pagination': bands, 'entity_type' : 'band', 'is_edit' : false} %}
            {% block navigaion %}

            {% endblock navigaion %}
        {% endembed %}
        {% endif %}
    </div>
{% endblock container%}
